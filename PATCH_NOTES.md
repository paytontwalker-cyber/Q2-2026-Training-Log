# Patch-Note Backlog

- Fix version auto-increment. It stopped at 1.9.0
- While iterating, show the current iteration version correctly
- History page still needs edit old logs
- Distance in Progress is not working correctly
- App needs reliable miles <-> meters conversion
- Consider reworking conditioning data entry to use selector + value fields instead of free typing units
- Consider structured conditioning logging similar to exercise logging
- Consider adding a Body Battery page/tab using recent 7-day volume, RPE, energy, and conditioning load
